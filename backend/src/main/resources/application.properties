spring.application.name=UVILLAGE Infractions
# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mariadb://localhost:3306/campusguard_db?useSSL=false
spring.datasource.username=root
spring.datasource.password=wijdane
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
spring.flyway.enabled=false

# JWT Configuration
app.jwtSecret=1P8AUePwuVIxdWTnjI6a+SjwU9IAz5ee7PRxzJUa9DVMpWe10mwD9ZOwQGDsiWzJySldBS3kKw1AK/gzxziEcA==
app.jwtExpirationMs=86400000        
app.jwtRefreshExpirationMs=604800000

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=lahboubwijdane@gmail.com
spring.mail.password=uapjhxjxpffxripi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.main.allow-bean-definition-overriding=true
# Note: The application runs at the root context. Controllers declare their
# own `/api` prefixes (for example `/api/auth`), so we avoid setting
# `server.servlet.context-path` here to prevent double `/api/api` mappings.